# 自动安装对等依赖 (Auto-install Peer Dependencies)
auto-install-peers=true

# 精确安装版本
save-exact=true

# ===================================================================
# Node_modules 结构与兼容性设置 (Node_modules Structure & Compatibility)
# ===================================================================

# 提升指定的包到根 node_modules (Hoist Specific Packages)
# 将所有 `@types` 相关的包提升到 `node_modules` 的根目录。
# 这是为了解决 pnpm 严格的依赖隔离策略导致 TypeScript 无法找到深层依赖的类型定义文件的问题。
# 这个配置可以有效避免大部分类型报错。
public-hoist-pattern[]=*@types*
public-hoist-pattern[]=@types/*

# 指定镜像源 (Specify Registry)
# 如果你或你的团队在国内，使用淘宝镜像源可以显著提升安装速度。
# registry=https://registry.npmmirror.com/